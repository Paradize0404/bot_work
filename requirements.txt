# Core
python-dotenv>=1.0.0
httpx>=0.27.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Telegram
aiogram>=3.10.0
aiohttp>=3.9.0

# Google Sheets
gspread>=6.0.0
google-auth>=2.20.0

# PDF generation
reportlab>=4.0.0

# Scheduler (ежедневная автосинхронизация)
apscheduler>=3.10.0

# Timezone data (нужен на Windows / Alpine для ZoneInfo)
tzdata>=2024.1

# OCR via OpenAI GPT-4o Vision
openai>=1.51.0

# Fuzzy matching (маппинг распознанных названий → номенклатура)
thefuzz>=0.22.0
python-Levenshtein>=0.25.0

# Image preprocessing
Pillow>=10.0.0

# XML parsing (stdlib, no extra dep)
