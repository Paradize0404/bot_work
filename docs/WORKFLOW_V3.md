# 🔄 Обновление workflow OCR: Кассир → Бухгалтер

## 🎯 Проблемы которые решили

### 1️⃣ **Ложные предупреждения о суммах**
**Было:**
```
⚠️ Предупреждения:
  • Поз.1: сумма 7512.38 ≠ кол-во(4.0) × цена(1972.0) = 7888.0
  • Поз.2: сумма 330.17 ≠ кол-во(3.0) × цена(134.27) = 402.81
  ...
```

**Проблема:** Система считала `qty × price`, но в документе может быть:
- Скидка
- Округление
- Сумма БЕЗ НДС уже указана правильно

**Решение:** 
- ✅ **Доверяем** `sum_without_vat` из документа
- ✅ Проверяем только **итоги** (расхождение > 5₽)
- ✅ Убрали значки ⚠️ после каждой позиции

### 2️⃣ **Кассир видел слишком много деталей**
**Было:**
```
[Кассир отправил фото]
↓
📄 Товарный чек
...29 позиций...
💰 Итого: 75861.09
⚠️ Предупреждения: ...

[Кнопки: ✅ Подтвердить | ❌ Отменить]
```

**Проблема:** Кассир тратит время на чтение и нажатие кнопок

**Решение:**
```
[Кассир отправил фото]
↓
✅ Загружено 4 фото.
📄 Документ отправлен бухгалтеру на проверку.
```
**Всё!** Кассир может работать дальше.

### 3️⃣ **Предупреждения должны видеть бухгалтеры, не кассиры**
**Теперь:**
- Кассир видит только: загружено/плохое качество
- Бухгалтер видит: полное превью + предупреждения + кнопки

---

## ✅ Новый workflow

### **Хорошее качество фото:**
```
1. Кассир отправил 4 фото
   ↓
2. Бот: "⏳ Получаю фото... (1) (2) (3) (4)"
   ↓
3. Бот: "⏳ Распознаю 4 страниц..."
   ↓
4. Gemini анализирует качество ✅
   ↓
5. Кассиру:
   ✅ Загружено 4 фото.
   📄 Документ отправлен бухгалтеру на проверку.
   ↓
6. Бухгалтеру:
   📄 Новый документ
   От: tg:123456789
   
   📄 УПД
   № Сборный
   Дата: 16.02.2026
   ...
   💰 Итого: 75861.09
   
   ⚠️ Итого с НДС: в документе 75861.09, 
      расчёт 75850.00 (разница 11.09)
   
   [Кнопки: ✅ Принято | ❌ Отклонить]
```

### **Плохое качество фото:**
```
1. Кассир отправил фото с бликами
   ↓
2. Бот распознал ✅
   ↓
3. Проверка качества ❌
   ↓
4. Кассиру:
   ⚠️ Проблемы с качеством фото
   
   🎯 Уверенность: 45%
   ❌ Причина: блик закрывает итоговую сумму
   
   📋 Проблемы:
     • Блик на позициях 5-7
   
   💡 Что делать:
   1. Уберите блики от лампы
   2. Держите камеру ровно
   ...
   
   📸 Отправьте новое фото
```

---

## 🔧 Технические изменения

### **use_cases/ocr_invoice.py**

#### `validate_and_fix()` — упрощена валидация:
```python
# ❌ БЫЛО: проверка qty × price (порождало ложные warning)
if qty × price ≠ sum_without_vat:
    warnings.append("Несовпадение...")

# ✅ СТАЛО: доверяем документу
if sum_without_vat is None:
    sum_without_vat = qty × price  # только если не указана

# Проверяем только итоги (разница > 5₽)
if abs(doc_total - calc_total) > 5.0:
    warnings.append("⚠️ Крупное расхождение...")
```

#### `format_preview()` — убраны маркеры ⚠️:
```python
# ❌ БЫЛО:
mismatch = " ⚠️" if item.get("_sum_mismatch") else ""
lines.append(f"{qty} × {price} = {sum}{mismatch}")

# ✅ СТАЛО:
lines.append(f"{qty} × {price} = {sum}")
# Предупреждения — только в конце, для бухгалтера
```

### **bot/ocr_handlers.py**

#### `_run_ocr()` — автоматическая отправка:
```python
# После проверки качества:
if quality_ok:
    # Сохраняем в БД
    doc_id = await save_ocr_result(tg_id, doc)
    
    # Кассиру: короткое подтверждение
    await placeholder.edit_text(
        f"✅ Загружено {count} фото.\n"
        f"📄 Документ отправлен бухгалтеру."
    )
    
    # Бухгалтеру: полное превью
    await _send_to_accountant_with_preview(bot, doc, doc_id, tg_id)
```

#### `_send_to_accountant_with_preview()` — новая функция:
```python
async def _send_to_accountant_with_preview(bot, doc, doc_id, sender_tg_id):
    """Отправить бухгалтеру полное превью с кнопками."""
    preview = format_preview(doc)  # включает предупреждения
    
    kb = InlineKeyboardMarkup([[
        InlineKeyboardButton("✅ Принято", f"ocr_ack:{doc_id}"),
        InlineKeyboardButton("❌ Отклонить", f"ocr_reject:{doc_id}"),
    ]])
    
    for acc_id in accountants:
        await bot.send_message(
            acc_id,
            f"📄 Новый документ\nОт: tg:{sender_tg_id}\n\n{preview}",
            reply_markup=kb,
        )
```

---

## 📊 Сравнение

| Критерий | Было | Стало |
|----------|------|-------|
| **Предупреждения qty×price** | На каждой позиции ⚠️ | Только крупные расхождения |
| **Workflow кассира** | Фото → Превью → Кнопки → Бухгалтер | Фото → "✅ Загружено" |
| **Время кассира** | 30-60 сек (читать, подтвердить) | 5 сек |
| **Предупреждения** | Видит кассир | Видит бухгалтер |
| **Кнопки у кассира** | ✅ Подтвердить / ❌ Отменить | Нет кнопок |
| **Превью** | У кассира и бухгалтера | Только у бухгалтера |

---

## 🎯 Преимущества

✅ **Кассир не отвлекается** — только "загружено" или "переснять"  
✅ **Меньше ложных предупреждений** — доверяем sum_without_vat из документа  
✅ **Бухгалтер видит всё** — полное превью + реальные расхождения (> 5₽)  
✅ **Быстрее** — кассир экономит 25-55 секунд на документ  
✅ **Понятнее** — каждому свою информацию  

---

## 🧪 Тестирование

### ✅ Сценарий 1: Хорошее фото
1. Кассир отправил 1 фото накладной
2. Должен получить: "✅ Загружено фото. Документ отправлен бухгалтеру"
3. Бухгалтер получил превью с кнопками

### ✅ Сценарий 2: Альбом
1. Кассир отправил 4 фото альбомом
2. Видит счётчик: "(1)" → "(2)" → "(3)" → "(4)"
3. Затем: "✅ Загружено 4 фото..."
4. Бухгалтер получил полное превью всех 4 страниц

### ✅ Сценарий 3: Плохое качество
1. Кассир отправил размытое фото
2. Получил: "⚠️ Проблемы с качеством... переснимите"
3. Отправил новое фото
4. Получил: "✅ Загружено фото..."

### ✅ Сценарий 4: Расхождение в суммах
1. Документ с итого: 75861.09
2. Расчёт по позициям: 75850.00
3. Кассиру: "✅ Загружено..."
4. Бухгалтеру: "⚠️ Итого с НДС: разница 11.09"

---

## 📝 Changelog

**2026-02-17 v3** — Переработан workflow:
- Кассир видит только статус загрузки/качества
- Бухгалтер получает полное превью с предупреждениями
- Убраны ложные предупреждения о qty×price
- Доверяем sum_without_vat из документа
- Проверка только крупных расхождений (> 5₽)
- Убраны кнопки для кассира
- Автоматическая отправка бухгалтеру

---

**Статус:** ✅ Готово к деплою
