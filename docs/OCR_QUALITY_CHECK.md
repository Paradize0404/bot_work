# 📸 Проверка качества фото при OCR

## 🎯 Цель
Автоматическая проверка качества фотографий документов **ДО** полного распознавания, чтобы избежать ошибок из-за бликов, размытости или обрезанных краёв.

---

## 🔄 Новый Workflow

### Одиночное фото:
```
1. Кассир отправляет фото документа
   ↓
2. Бот: "⏳ Распознаю документ..."
   ↓
3. ⚡ Gemini анализирует качество + распознаёт
   ↓
4. Решение:
   ✅ Качество OK → Превью → Отправка бухгалтеру
   ❌ Плохое → Показать проблемы → Просит переснять
```

### Альбом (пачка фото):
```
1. Кассир отправляет пачку фото (альбом из 5 штук)
   ↓
2. Бот получает их по одному и показывает:
   "⏳ Получаю фото... (1)"
   "⏳ Получаю фото... (2)"
   "⏳ Получаю фото... (3)"
   ...
   "⏳ Получаю фото... (5)"
   ↓
3. Пауза 2 секунды после последнего фото
   ↓
4. "⏳ Распознаю 5 страниц..."
   ↓
5. ⚡ Gemini анализирует ВСЮ пачку РАЗОМ
   ↓
6. Решение:
   ✅ Качество OK → Превью → Отправка бухгалтеру
   ❌ Плохое → Показать проблемы → Просит переснять ВСЁ
```

**Ключевое улучшение**: 
- ✅ Бот показывает ОДНО сообщение с живым счётчиком фото
- ✅ Отправка в нейросеть происходит РАЗОМ после сбора всех фото
- ✅ Нет дублирующих сообщений "Распознаю X страниц..."

---

## 📊 Структура проверки качества

### В промпте Gemini добавлен блок:

```
⚠️ ПРОВЕРКА КАЧЕСТВА ФОТО (ОБЯЗАТЕЛЬНО):
- Оцени качество фото перед распознаванием
- Проверь наличие бликов, размытости, обрезанных краёв
- Оцени свою уверенность в правильности распознавания (0-100%)
- Если уверенность < 70% ИЛИ есть критические проблемы → укажи needs_retake: true
```

### JSON-ответ LLM включает:

```json
{
  "quality_check": {
    "is_readable": true,          // текст читаемый
    "has_glare": false,            // есть блики
    "has_blur": false,             // есть размытость
    "is_complete": true,           // документ целиком в кадре
    "confidence_score": 95,        // уверенность LLM (0-100%)
    "issues": [                    // список проблем
      "лёгкая размытость в правом верхнем углу",
      "блик на позиции №7"
    ],
    "needs_retake": false,         // нужно ли переснимать
    "retake_reason": "блики закрывают итоговую сумму"
  },
  "doc_type": "Кассовый чек",
  // ... остальные поля как обычно
}
```

---

## 🚨 Критерии отклонения фото

### Автоматическое отклонение (needs_retake = true):

1. **Низкая уверенность**: `confidence_score < 70%`

2. **Критические флаги**:
   - `is_readable = false` — текст нечитаемый
   - `has_glare = true AND has_blur = true` — одновременно блики и размытость
   - `is_complete = false` — обрезаны края документа

3. **Множественные проблемы**: `len(issues) >= 3`

4. **LLM сам решил**: модель указала `needs_retake: true`

---

## 💬 Сообщения пользователю

### При плохом качестве:

```
⚠️ Проблемы с качеством фото

🎯 Уверенность распознавания: 45%

❌ Причина: блики закрывают итоговую сумму

📋 Обнаруженные проблемы:
  • Сильный блик на позициях 5-7
  • Размытость в нижней части чека
  • Обрезан правый край

💡 Что делать:
1. Убедитесь что нет бликов от лампы/окна
2. Держите камеру ровно над документом
3. Проверьте что весь документ в кадре
4. Сделайте фото заново в хорошо освещённом месте

📸 Отправьте новое фото или нажмите «❌ Отменить»
```

### При хорошем качестве:

Стандартное превью документа с кнопками:
- ✅ Отправить бухгалтеру
- ❌ Отменить

---

## 🔧 Технические детали

### Файлы:

1. **adapters/gemini_vision.py**
   - Модифицирован `_SYSTEM_PROMPT` — добавлен блок проверки качества
   - JSON-схема включает `quality_check`

2. **use_cases/ocr_invoice.py**
   - `check_photo_quality(doc)` — анализ quality_check из JSON
   - `format_quality_message(quality_result)` — форматирование сообщений об ошибках

3. **bot/ocr_handlers.py**
   - `OcrStates.waiting_retake` — новый FSM-state для переснятых фото
   - `ALBUM_WAIT_SEC = 2.0` — время ожидания завершения альбома
   - `_collect_album_photo()` — сбор фото с живым счётчиком
   - `_album_timer()` — таймер, обновляет сообщение перед распознаванием
   - `_run_ocr()` — проверка качества после распознавания (принимает status_message)
   - `_run_ocr_from_album()` — обёртка для вызова из сборщика альбомов
   - `handle_retake_photo()` — обработчик переснятого фото
   - `_send_to_accountant_simple()` — упрощённая отправка бухгалтеру (БЕЗ маппинга)

### FSM States:

```
waiting_photo      → ожидаем первое фото
    ↓
[OCR + проверка качества]
    ↓
waiting_retake     → ожидаем переснятое фото (если плохое качество)
    ↓ (если OK)
preview            → показываем превью + кнопки
    ↓
[отправка бухгалтеру]
```

---

## 📈 Преимущества

✅ **Меньше ошибок** — отсекаем плохие фото сразу  
✅ **Экономия времени** — не нужно вручную проверять качество  
✅ **Обучение пользователя** — конкретные рекомендации что исправить  
✅ **Меньше переделок** — бухгалтер получает только читаемые документы  

---

## 🧪 Примеры проблем

| Проблема | Флаг | needs_retake | Причина |
|----------|------|--------------|---------|
| Сильный блик на сумме | `has_glare: true` | ✅ true | Критичная информация не видна |
| Лёгкая размытость углов | `has_blur: true` | ❌ false | Основной текст читаемый |
| Обрезана половина чека | `is_complete: false` | ✅ true | Документ неполный |
| Уверенность 95% | — | ❌ false | Отличное качество |
| Уверенность 55% | — | ✅ true | Низкая уверенность |
| 3+ мелких проблемы | `len(issues) >= 3` | ✅ true | Накопительный эффект |

---

## 🚀 Следующие шаги

1. ✅ **Проверка качества** — ГОТОВО
2. ✅ **Упрощённая отправка бухгалтеру** — ГОТОВО (без маппинга)
3. ⏳ **Тестирование на реальных фото** — нужно проверить на чеках
4. ⏳ **Статистика качества** — собирать метрики: сколько фото отклонено, типичные проблемы
5. ⏳ **Fine-tuning порогов** — возможно, 70% слишком строго/мягко
6. ⏳ **Возврат маппинга** — после отладки качества вернуть автоматический маппинг товаров

---

## 📝 Changelog

**2026-02-17 (v2)** — Улучшена логика альбомов:
- Живой счётчик фото: "⏳ Получаю фото... (1)" → "(2)" → "(3)"
- Пауза 2 секунды после последнего фото (было 1.5)
- Отправка в нейросеть РАЗОМ после сбора всех фото
- Исправлено дублирование сообщений "Распознаю X страниц..."
- Одно сообщение обновляется на протяжении всего процесса

**2026-02-17 (v1)** — Добавлена автоматическая проверка качества фото:
- Gemini анализирует блики, размытость, полноту документа
- Автоматическое отклонение фото с confidence < 70%
- Детальные сообщения о проблемах + рекомендации
- Упрощённый workflow: фото → проверка → бухгалтер (без маппинга пока)
