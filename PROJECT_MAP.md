# 🗂 Карта проекта iiko + FinTablo Sync Bot

> Последнее обновление: 2026-02-13
> Язык общения с разработчиком: **русский**

---

## 🎯 О проекте

Telegram-бот для синхронизации справочных данных из **iiko REST API** и **FinTablo REST API** в **PostgreSQL**.
Это **скелет большого проекта** — впереди новые интеграции, источники данных, аналитика.
Текущая фаза: выгрузка всех справочников iiko + FinTablo в БД по кнопкам бота + **авторизация сотрудников через Telegram** + **акты списания с проверкой админами** + **загрузка накладных** + **управление администраторами бота** + **синхронизация остатков по складам** + **проверка минимальных остатков по подразделениям** + **история списаний с повтором и фильтрацией по ролям** + **расходные накладные из шаблонов с прайс-листом из БД** + **XML-импорт в iiko** + **заявки на товары (создание → одобрение → расходная накладная в iiko) + история заявок с дублированием** + **гранулярные права доступа через Google Таблицу** + **PDF-документы расходных накладных и заявок (2 копии, адаптивный размер, кириллица)** + **глобальная отмена /cancel + NavResetMiddleware (защита от зависания FSM)**.

---

## 🗺 Навигация по документации

> **Этот файл (PROJECT_MAP.md) читается ВСЕГДА.** Дополнительные файлы — по задаче.

| Задача | Что читать дополнительно |
|--------|--------------------------|
| Новая фича, рефактор, поиск модуля | [`docs/FILE_STRUCTURE.md`](docs/FILE_STRUCTURE.md) — дерево файлов, описания модулей, FSM-потоки, кнопки бота |
| Миграция, новая таблица, sync-задача, работа с данными | [`docs/DATABASE.md`](docs/DATABASE.md) — все 31 схем таблиц |
| Новый sync, внешний API, производительность, Railway | [`docs/API_INTEGRATIONS.md`](docs/API_INTEGRATIONS.md) — iiko/FinTablo API, инфраструктура, оптимизации, грабли |
| Что уже было сделано, история решений | [`docs/CHANGELOG.md`](docs/CHANGELOG.md) — все изменения с датами |
| Застрял, баг не решается, отладка | [`docs/KNOWN_ISSUES.md`](docs/KNOWN_ISSUES.md) — решённые проблемы и грабли |
| Безопасность, валидация, конкурентность, деплой | [`docs/SECURITY_AND_RELIABILITY.md`](docs/SECURITY_AND_RELIABILITY.md) — паттерны защиты с примерами кода |
| Новый handler, UX, скорость отклика, кеширование | [`docs/UX_PATTERNS.md`](docs/UX_PATTERNS.md) — паттерны быстрого и чистого UI с примерами кода |
| Куда писать новый changelog | [`docs/CHANGELOG.md`](docs/CHANGELOG.md) — добавлять сверху (новое первым) |

---

## 🧠 Архитектурные принципы (ОБЯЗАТЕЛЬНО соблюдать)

1. **Тонкие хэндлеры** — `bot/handlers.py` только принимает команду, вызывает use_case, отправляет ответ. Никакой бизнес-логики.
2. **Бизнес-логика в use_cases/** — sync, валидация, трансформация данных.
3. **Интеграции в adapters/** — HTTP, XML, внешние API. Use_cases не знают про HTTP/XML.
4. **Никакого хардкода** — все секреты и настройки в `.env`, читаются через `config.py`.
5. **1 кнопка = 1 таблица** — не плодить таблицы без необходимости.
6. **UPSERT-паттерн** — INSERT ON CONFLICT DO UPDATE, батчами по 500 строк.
7. **Mirror-sync** — после UPSERT, DELETE из БД записей, которых больше нет в API. БД = зеркало iiko/FinTablo.
8. **raw_json в каждой таблице** — страховка: полный ответ API хранится как JSONB.
9. **SyncLog — аудит всего** — каждая синхронизация пишется в iiko_sync_log.
10. **Логирование везде** — тайминги API-запросов, прогресс batch, итоги. Verbose, не silent.
11. **Быстрый старт бота** — `SELECT 1` вместо `create_all` при каждом запуске.

---

## 🏗 Конвенции разработки

- **Python 3.12**, async everywhere (asyncio, asyncpg, httpx, aiogram 3)
- **SQLAlchemy 2.0** declarative models, async session
- **Маленькие изменения** — не переписывать всё сразу, инкрементальные правки
- **DRY** — generic `_run_sync()` + `_batch_upsert()` вместо копипасты на каждую таблицу
- **Параллельность** — `asyncio.gather` для независимых API-запросов и sync-операций
- **Persistent HTTP client** — один `httpx.AsyncClient` с connection pool, не создавать новый на каждый запрос

---

## 🚫 Антипаттерны и запреты (НЕ ДЕЛАТЬ)

### Python / asyncio
- **НЕ использовать** `asyncio.get_event_loop()` — deprecated с Python 3.10+. Использовать `asyncio.run()` или `asyncio.get_running_loop()`
- **НЕ использовать** синхронные блокирующие вызовы (`time.sleep`, `requests`) в async-коде — только `asyncio.sleep`, `httpx.AsyncClient`
- **НЕ использовать** `print()` — только `logging` (уже настроен через `logging_config.py`)
- **НЕ использовать** `typing.Optional` — `X | None` (Python 3.12 syntax)
- **НЕ глотать исключения** — `except Exception: pass` запрещён. Логировать всё через `logger.exception()`

### URLs / сети
- **Все внешние URL** должны начинаться с `https://` — если переменная может прийти без схемы, добавлять автоматически (см. `WEBHOOK_URL` в `config.py`)
- **DATABASE_URL** — нормализовать префикс под `asyncpg` (уже сделано в `config.py`). Никогда не полагаться на то, что пользователь задаст правильный драйвер

### Деплой / Railway
- **Procfile: `web`** для webhook-бота (Railway должен пробросить порт), `worker` — только для polling без порта
- **PORT** — всегда читать из `os.getenv("PORT")`, Railway задаёт свой. Не хардкодить
- **`0.0.0.0`** — слушать на всех интерфейсах, не на `127.0.0.1` (контейнер)
- **Секреты** — только через переменные окружения Railway. Никогда не коммитить `.env`
- **Internal vs Public DB URL** — на Railway использовать `railway.internal` (быстрее), локально — публичный proxy

### Telegram Bot
- **Webhook** — только `https://`, Telegram не принимает `http://`
- **`drop_pending_updates=True`** при set_webhook / start_polling — чтобы не обрабатывать старые сообщения после рестарта
- **delete_webhook** перед polling — иначе Telegram продолжит слать апдейты на старый вебхук и polling не получит ничего

### БД / SQLAlchemy
- **Не создавать** engine на уровне функции — только singleton в `db/engine.py`
- **`expire_on_commit=False`** — обязательно для async-сессий, иначе lazy load после commit крашнет
- **`pool_pre_ping=True`** — обязательно для Railway (дропает idle-соединения)
- **Миграции** — `IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` для идемпотентности

---

## 🛡 Guardrails масштабирования и защиты от регрессий (ОБЯЗАТЕЛЬНО)

### 1) Контракт изменений (любой код, особенно с AI)
- Любое изменение = (а) цель/симптом → (б) минимальный план → (в) правка use_cases/adapters → (г) тонкий handler → (д) тест/проверка → (е) обновить документацию.
- Запрещены "тихие" рефакторы: если не запрошено явно — не трогать имена, интерфейсы, структуру модулей.
- Каждая правка должна быть наблюдаемой: +лог на входе и лог итогов (ok/error + тайминг).

### 2) Definition of Done (DoD) для любой фичи/фикса
- ✅ Есть воспроизводимость: шаги или лог/stacktrace + входные данные
- ✅ Есть защита от регрессии: хотя бы один тест/смоук-скрипт или "контракт" (моки API + проверка результата)
- ✅ Логи содержат минимум: tg_id, employee_id/department_id (если есть), doc_id (если есть)
- ✅ Нет утечек секретов/токенов в логах
- ✅ Изменение не увеличило round-trip'ы к БД без причины (Railway latency)
- ✅ Документация обновлена (если изменились интерфейсы, модули, схема БД)

### 3) Инварианты данных и внешних API
- Все внешние операции должны быть идемпотентны или иметь защиту от дублей (повторный клик/ретрай/таймаут).
- Любой retry = exponential backoff + лимит попыток + лог причины.
- Любой парсинг/маппинг данных должен сохранять raw_json (если сущность хранится в БД) и быть устойчив к отсутствующим полям.

### 4) Mirror-delete: безопасные правила
- Mirror-delete разрешён только если:
  - API-ответ валиден (200/ok) и список IDs не пуст,
  - sanity-check пройден: удаляем не более 50% от текущего размера таблицы за одну синхронизацию,
  - при провале sanity-check: skip + warning + запись в SyncLog как error/partial (без удаления).
- Всегда выполнять delete в той же транзакции что и upsert + sync_log.

### 5) Состояния и конкурентность
- Любое "in-memory state" (pending docs, locks, кеши) имеет потолок:
  - 1 инстанс бота и рестарт = допустимая потеря состояния (это ОК на текущем этапе).
  - Если нужен HA/2+ инстанса/гарантии — перенос состояния в БД/Redis.
- Все пользовательские флоу должны быть устойчивы к: double-click, повтору сообщения, таймаутам, частичной потере state.

### 6) Схема БД
- Изменение схемы = миграция через `_MIGRATIONS` в `init_db.py` (с `IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS`).
- Любой destructive-change (DROP/DELETE столбцов/таблиц) требует отдельного ручного подтверждения и логирования.
- Переход на Alembic — при необходимости rollback-миграций или когда количество миграций превысит 10+.

### 7) Валидация входных данных (TRUST NOTHING)
- **callback_data** — ВСЕГДА валидировать после `split(":", 1)[1]`:
  - UUID значения: `try: UUID(value) except ValueError: await callback.answer("Ошибка данных"); return`
  - int значения: `try: int(value) except ValueError: ...`
  - Никогда не передавать сырой callback_data напрямую в БД-запрос без валидации формата.
- **Текстовый ввод** — ограничивать длину (имена ≤100, причины ≤500, общий текст ≤2000 символов).
- **FSM-переходы** — проверять авторизацию/admin-права на **каждом** шаге FSM, а не только на входе в flow.
  Если пользователь потерял права в процессе — graceful exit с сообщением.
- **Числовые параметры** — проверять границы (min/max) и тип до передачи в бизнес-логику.
- **Правило**: если данные приходят от пользователя (callback, text, inline query) — это **враждебный ввод** до проверки.

### 8) Безопасность
- **Webhook secret** — ОБЯЗАТЕЛЕН `secret_token` при `set_webhook()`. Генерировать через `secrets.token_hex(32)`, хранить в env.
  Без secret любой, кто знает webhook URL, может инжектировать фейковые Telegram-апдейты.
- **Авторизация sync-операций** — все кнопки синхронизации (iiko/FinTablo) = **admin-only** или как минимум авторизованный пользователь.
  Sync — дорогая операция (API-запросы + batch DB write). Неавторизованный пользователь не должен иметь доступа.
- **Гранулярные права доступа** — 3 уровня защиты для каждого раздела:
  1. **Клавиатура:** `_main_keyboard(allowed)` — кнопки без прав **не показываются** пользователю.
  2. **Декоратор:** `@permission_required(perm_key)` на handler — даже если кнопку прислали напрямую, доступ заблокирован.
  3. **Админ bypass:** админы имеют все права автоматически (не нужно ставить ✅ в таблице).
  Матрица прав — Google Таблица (лист «Права доступа»), кеш в памяти (TTL 5 мин).
  Кнопка «🔑 Права доступа → GSheet» — выгрузка сотрудников + столбцов прав (админ-only).
- **Токены в логах** — iiko API передаёт `key` в URL query params. При `LOG_LEVEL=DEBUG` httpx логирует полные URL.
  Правило: httpx/httpcore логгеры = минимум `WARNING`. Никогда не понижать. В error-сообщениях маскировать токены.
- **Admin self-removal** — запретить удаление последнего админа. Проверка: `if count(admins) <= 1 and target == self: deny`.
- **`/admin_init` атомарность** — проверка «таблица пуста?» и вставка должны быть в одной транзакции. Иначе race condition при двух одновременных вызовах.
- **Rate limiting** — aiogram `ThrottlingMiddleware` или ручной cooldown (1 sec между sync-кнопками, 0.5 sec между обычными).

### 9) Health-мониторинг и алертинг
- **`/health` endpoint** — обязателен в webhook-режиме. Проверяет: (1) DB `SELECT 1`, (2) возвращает `200 OK` + JSON со статусом.
  Railway и любой балансировщик используют health-check для перезапуска мёртвых контейнеров.
- **Критические ошибки → уведомление** — при `logger.exception()` в бизнес-логике отправлять сообщение админам (Telegram).
  Логи на Railway никто не мониторит в реальном времени. Алерт в Telegram — бесплатный мониторинг.
- **Staleness detection** — если последний `SyncLog` старше 24ч, предупреждение при следующем запуске бота или по расписанию.
- **Startup self-check** — при старте бота проверять: DB alive, iiko token получается, FinTablo доступен. Fail-fast если нет.

### 10) Graceful Shutdown & Целостность состояния
- **Pending writeoffs → в БД** — in-memory `_pending` dict теряется при рестарте. Для production: хранить в таблице `pending_writeoffs` с TTL.
  Пока в RAM — принять риск, но **логировать при shutdown** что именно потеряно (doc_id, user_id).
- **SIGTERM** — Railway шлёт `SIGTERM`, не `KeyboardInterrupt`. Добавить `signal.signal(SIGTERM, handler)` для polling-режима.
- **Background tasks** — все `asyncio.create_task()` регистрировать в `set()`.
  При shutdown: `await asyncio.gather(*tasks, return_exceptions=True)` с timeout 10 сек.
- **Cleanup order**: (1) остановить приём обновлений → (2) дождаться background tasks → (3) flush pending state → (4) закрыть HTTP-клиенты → (5) dispose DB engine.

### 11) Конкурентность операций
- **Sync-lock** — `asyncio.Lock()` per entity type. Одна и та же синхронизация не может запускаться параллельно двумя пользователями.
  При попытке: `await callback.answer("⏳ Синхронизация уже запущена"); return`.
- **Double-click на дорогие операции** — паттерн: `if user_id in _lock: answer("⏳"); return` → `_lock.add(user_id)` → `try: ... finally: _lock.discard(user_id)`.
- **Atomic admin operations** — add/remove admin = одна DB-транзакция + invalidate cache. Не разделять.
- **Write-after-read consistency** — если читаешь из кеша и пишешь по результату, убедись что кеш не устарел (re-fetch если TTL > 50%).

### 13) UX: скорость и чистота чата (ОБЯЗАТЕЛЬНО)
- **Одно окно:** inline-кнопки → `edit_text()`, НИКОГДА `answer()`. Reply-кнопки → delete old + send new (через `_menu_msg_id` в state).
- **Edit-first паттерн:** `_send_prompt()` и `_update_summary()` сначала `edit_message_text()`, fallback на `send_message()`. **Не удалять сообщение + отправлять новое** — только edit.
- **`/cancel` → edit prompt:** при отмене — редактировать prompt-сообщение, а не удалять + присылать новое.
- **NavResetMiddleware:** outer-middleware на `dp.message`, перехватывает 55+ Reply-кнопок навигации при активном FSM → сброс + cleanup → кнопка проходит к штатному хэндлеру. Исключает «зависание» бота.
- **Cancel-keyboard во flow'ах:** при входе в dialog-flow → `set_cancel_kb()` скрывает Reply-клавиатуру подменю и показывает только «❌ Отмена». При выходе → `restore_menu_kb()` восстанавливает клавиатуру подменю. Keyboard-функции — в `bot/_utils.py`.
- **Placeholder → edit:** любая операция >1 сек → `"⏳ ..."` placeholder → `edit_text("✅ результат")`. Не отправлять результат отдельным сообщением.
- **Прогресс длинных операций (>5 сек):** обновлять placeholder пошагово («✅ Справочники... ⏳ Номенклатура...»).
- **ChatAction.typing** — перед любой DB/API операцией в text-handler'e где нет inline-кнопок.
- **Текст пользователя → удалять ВСЕГДА** (`try: await message.delete()`). Нет исключений.
- **Ошибки валидации → edit prompt**, не новое сообщение. 5 ошибок подряд = 1 сообщение бота, не 5.
- **Prefetch** — при входе в раздел грузить данные следующего шага фоном (`asyncio.create_task`).
- **Cache warmup** — при старте бота прогревать user_context и admin_ids.
- **user_context TTL** — 30 мин (не бесконечный). Иначе изменения в iiko не отражаются.
- Подробные паттерны с кодом — см. `docs/UX_PATTERNS.md`.

### 14) Чеклист деплоя (обязательный)

**Перед деплоем:**
- [ ] Все env vars заданы (проверить `config.py _require()`)
- [ ] DB миграции применены (`python -m db.init_db`)
- [ ] Нет `print()`, нет hardcoded secrets, нет `LOG_LEVEL=DEBUG` в production
- [ ] Health endpoint отвечает (если webhook-режим)
- [ ] `Procfile` указывает правильный тип (`web` для webhook, `worker` для polling)
- [ ] Webhook URL корректен и использует `https://`

**После деплоя:**
- [ ] Бот отвечает на `/start` в течение 5 сек
- [ ] Health endpoint возвращает `200`
- [ ] SyncLog пишется корректно (запустить любую sync вручную)
- [ ] Логи чистые на INFO-уровне (нет токенов, нет трейсбеков)
- [ ] Webhook зарегистрирован с `secret_token`

---

## ⚙️ Переменные окружения (.env)

| Переменная | Обязат. | Описание |
|------------|---------|-------------------------------------------|
| `DATABASE_URL` | ✅ | PostgreSQL connection string (`postgresql+asyncpg://...`) |
| `IIKO_BASE_URL` | ✅ | Базовый URL iiko API (`https://ip-merzlyakov-e-a-co.iiko.it`) |
| `IIKO_LOGIN` | ✅ | Логин iiko API |
| `IIKO_SHA1_PASSWORD` | ✅ | SHA1-хеш пароля iiko |
| `FINTABLO_TOKEN` | ✅ | Bearer-токен FinTablo API |
| `TELEGRAM_BOT_TOKEN` | ✅ | Токен Telegram-бота (от @BotFather) |
| `OPENAI_API_KEY` | ✅ | Ключ OpenAI (GPT Vision OCR для накладных) |
| `FINTABLO_BASE_URL` | ❌ | Base URL FinTablo (дефолт `https://api.fintablo.ru`) |
| `WEBHOOK_URL` | ❌ | URL сервиса на Railway (`https://xxx.up.railway.app`). Если задан — webhook, иначе polling |
| `WEBHOOK_PATH` | ❌ | Путь вебхука (дефолт `/webhook`) |
| `PORT` | ❌ | Порт веб-сервера (Railway задаёт автоматически, дефолт `8080`) |
| `LOG_LEVEL` | ❌ | Уровень логирования (дефолт `INFO`) |
| `INVOICE_PRICE_SHEET_ID` | ❌ | Google Sheet ID для прайс-листа (цены поставщиков → БД) |

Все обязательные переменные читаются через `_require()` в `config.py` — **fail-fast** с понятной ошибкой если пусто.

---

## 🛠 Команды

```bash
# Установка зависимостей
pip install -r requirements.txt

# Создание таблиц
python -m db.init_db

# Запуск бота
python main.py
```

---

## 🔮 Планы / следующие шаги

- Это будет **большой проект с различными интеграциями**
- Возможные направления: аналитика, отчёты, другие API-источники
- Архитектура уже готова к масштабированию: adapters/ для новых интеграций, use_cases/ для логики
- FinTablo: scheduler для автоматической периодической синхронизации
- Связки iiko ↔ FinTablo: матчинг контрагентов/сотрудников между системами

---

## 📋 Как работать с этим проектом (для AI-ассистента)

1. **Всегда читай этот файл первым** при начале нового диалога. Дополнительные docs/ — по таблице навигации выше.
2. Соблюдай архитектурные принципы — не лепи логику в handlers.
3. **Порядок изменений (строго):** анализ существующего кода → use_case (бизнес-логика) → handler (тонкий) → обновить документацию. Никогда не начинай с handler'а.
4. При оптимизации: помни про Railway latency — каждый лишний round-trip = +400мс.
5. **Логирование — ОБЯЗАТЕЛЬНО для любого нового кода:**
   - Каждый новый handler **ОБЯЗАН** иметь `logger.info()`/`logger.debug()` на входе
   - Формат: `[module] действие tg:%d, params` — где module один из: `[auth]`, `[nav]`, `[sync]`, `[sync-ft]`, `[bg]`, `[writeoff]`, `[writeoff-edit]`, `[admin]`, `[invoice]`, `[perm]` (или новый по аналогии)
   - Значимые действия (кнопки, ввод, выбор) → `logger.info`
   - Пагинация, guard-хэндлеры, noop → `logger.debug`
   - Фоновые задачи: лог на старте и на завершении (с таймингом)
   - Бизнес-логика в use_cases: лог с таймингом (`time.monotonic()`)
   - Ошибки: `logger.warning`/`logger.exception` с контекстом (tg_id, doc_id и т.д.)
   - **Никогда не создавай handler без entry-лога** — это главное правило
6. **Кеширование — 4 уровня:**
   - **In-memory dict** (user_context, admin_ids) — для данных, которые живут всю сессию бота. Инвалидация ручная.
   - **TTL-кеш** (writeoff_cache, permissions) — для данных из БД/GSheet, которые могут устареть. TTL 5–30 мин.
   - **FSM state.data** — для данных внутри одного FSM-флоу (`_stores_cache`, `_accounts_cache`). Живёт до `state.clear()`.
   - **GSheet → in-memory** (permissions) — матрица прав из Google Таблицы, без промежуточной БД. TTL 5 мин + graceful degradation.
   - Перед созданием нового кеша — проверь, подходит ли один из существующих.
7. **Параллелизация — правило, а не оптимизация:**
   - Если 2+ независимых async-вызова идут подряд → `asyncio.gather()` сразу, не «потом оптимизируем»
   - Фоновые задачи: `asyncio.create_task()` + `try/except` + лог. НИКОГДА не `await` для того что не должно блокировать пользователя.
8. **Fallback — всегда graceful degradation:**
   - Нет данных в кеше → запрос в БД (а не ошибка)
   - Нет админов → прямая отправка (а не «невозможно»)
   - Авто-выбор не сработал → ручной выбор (а не сбой)
   - Нет unit_name → «шт» (а не пустая строка)
9. **Telegram UX — скорость и чистота чата (ОБЯЗАТЕЛЬНО):**
   - `callback.answer()` — **ПЕРВАЯ строка** в каждом callback-handler (снимает спиннер). Никогда не ставь после DB/API вызовов.
   - **Одно окно:** inline → `edit_text()`, НИКОГДА `answer()`. Reply-меню → `delete old msg` → `send new` (через tracked `_menu_msg_id`).
   - **Текст пользователя → удалять ВСЕГДА** — `try: await message.delete()` первой строкой в text-handler.
   - **Ошибки валидации → edit prompt** (не answer). 5 ошибок подряд = 1 сообщение, не 5.
   - **Placeholder → edit:** `"⏳ ..."` → долгая операция → `edit_text("✅ результат")`. Результат НЕ новым сообщением.
   - **ChatAction.typing** — в text-handler перед любым DB/API вызовом (>0.5 сек).
   - Эмодзи в начале сообщений (✅ ❌ ⏳ ⚠️ 📄 🏨 📂 🔍 📏)
   - Guard-хэндлеры: delete текст + **edit prompt** с подсказкой (не answer)
   - Пагинация: ◀️ ▶️ + счётчик «N/M», всегда `edit_text` (не новое сообщение)
   - **Подробные паттерны с кодом —** `docs/UX_PATTERNS.md`
10. **Обновление документации — обязательный финальный шаг:**
    - После КАЖДОГО изменения функциональности — обнови соответствующие секции в docs/
    - Добавь запись в `docs/CHANGELOG.md` (сверху, новое первым)
    - Если добавил новый файл — добавь его в `docs/FILE_STRUCTURE.md`
    - Если изменил схему БД — обнови `docs/DATABASE.md`
    - Если изменил API/оптимизации — обнови `docs/API_INTEGRATIONS.md`
11. **Фиксация проблем — если потратил >10 мин на баг, запиши в `docs/KNOWN_ISSUES.md`:**
    - Формат: **Симптом** → **Причина** → **Решение** → **Как избежать**
    - Это база знаний проекта — перед отладкой сначала загляни туда
    - Записывай даже «глупые» ошибки — они повторяются чаще всего
12. **Валидация callback_data — ОБЯЗАТЕЛЬНА в каждом новом handler:**
    - `callback.data.split(":", 1)[1]` → через `_parse_uuid()` / `_parse_int()` → проверка None → early return
    - Невалидные данные → `callback.answer("⚠️ Ошибка")` + `logger.warning` (детекция атак)
    - Паттерны и хелперы — см. `docs/SECURITY_AND_RELIABILITY.md` §1
13. **Безопасность — при любом новом handler/endpoint:**
    - Проверь матрицу доступа: кто может вызвать? (неавториз. / авториз. / по perm_key / админ)
    - Дорогие операции (sync, POST) = **admin-only** + rate limiting
    - Разделы меню = `@permission_required("perm_key")` (права из Google Таблицы)
    - Никогда не логируй URL с `key=` / `token=` / `Bearer` на уровне ниже WARNING
    - Подробные паттерны — см. `docs/SECURITY_AND_RELIABILITY.md` §2–§5
14. **Конкурентность — при создании дорогой async-операции:**
    - Sync-операции: через sync-lock (`asyncio.Lock` per entity), не допускать параллельный запуск
    - Background tasks: регистрировать в `_background_tasks` set для graceful shutdown
    - Паттерны — см. `docs/SECURITY_AND_RELIABILITY.md` §7–§8
15. **UX — при создании любого handler'a:**
    - Inline callback → `edit_text`, никогда `answer` (кроме callback.answer)
    - Текст пользователя → `message.delete()` первым делом
    - Ошибка валидации → edit prompt (не новое сообщение)
    - Долгая операция → `"⏳"` placeholder → edit с результатом
    - Подробные паттерны и код — `docs/UX_PATTERNS.md`
16. **Prefetch & cache warmup:**
    - При входе в раздел → `asyncio.create_task(preload_next_step_data())`
    - При старте бота → `_warmup_caches()` (см. `docs/UX_PATTERNS.md` §6)
    - `user_context` = TTL 30 мин, не бесконечный
17. Не создавай лишних файлов (markdown-отчёты, скрипты) — только то что просят
18. Общайся на русском, кратко, по существу

---

## ✅ Чеклист для нового кода (копируй в промпт при запросе фичи)

> **Как использовать:** скопируй блок ниже в начало промпта перед описанием задачи.
> Это заставит AI держать каждый пункт в фокусе при генерации кода, а не «где-то в документе было».

```
Прочитай PROJECT_MAP.md. При реализации ОБЯЗАТЕЛЬНО соблюдай:

АРХИТЕКТУРА:
- [ ] Тонкий handler → бизнес-логика в use_cases/ → адаптеры в adapters/
- [ ] Порядок: анализ → use_case → handler → документация

ВАЛИДАЦИЯ:
- [ ] callback_data — try/except (ValueError, IndexError) на каждый int()/UUID()
- [ ] Текстовый ввод — ограничить длину
- [ ] FSM — проверять авторизацию на КАЖДОМ шаге, не только на входе

ЛОГИРОВАНИЕ:
- [ ] logger.info с tg_id в КАЖДОМ хэндлере на входе
- [ ] logger.warning/exception с контекстом при ошибках
- [ ] Никаких except Exception: pass без логирования

TELEGRAM UX:
- [ ] callback.answer() — ПЕРВАЯ строка callback-handler
- [ ] Inline → edit_text(), НИКОГДА answer()
- [ ] Текст пользователя → message.delete() первым делом
- [ ] Долгая операция → ⏳ placeholder → edit с результатом

ДОКУМЕНТАЦИЯ (финальный шаг):
- [ ] docs/FILE_STRUCTURE.md — новые файлы, FSM-состояния, кнопки
- [ ] docs/DATABASE.md — новые таблицы, счётчики
- [ ] docs/CHANGELOG.md — запись сверху с датой
- [ ] PROJECT_MAP.md — обновить описание фазы и навигацию
```